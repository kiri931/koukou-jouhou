#include<stdio.h>

int main() {
    int a,b;
    printf("２つの整数を入力してください: ");
    
    scanf("%d %d", &a,&b);

    printf("%d : %dと入力されました。\n",a,b);

    return 0;
}

// これでC言語のプログラムを行います。

// scanfを実際に書いてみます

// ちなみにscanfは２つの入力を読み取ることができます。
